/* ============================================================
   MTK Property — Property Management System
   Runs on every Desk page load (app_include_js).
   ============================================================ */
